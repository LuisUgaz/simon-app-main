import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginBottom: 16,
  },
  instructions: {
    color: '#666666',
    marginBottom: 8,
    fontSize: 14,
  },
  input: {
    backgroundColor: '#ffffff',
    marginTop: 8,
    minHeight: 120,
  },
  counter: {
    textAlign: 'right',
    fontSize: 12,
    color: '#666666',
    marginTop: 4,
  },
}); 